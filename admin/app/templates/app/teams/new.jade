.row
  .small-12.columns
    .vz-panel
      form(novalidate)
        input(type='text', ng-model='data.team.name')
        br
        button(ng-click='cancel()') Cancel
        button(ng-click='create()') Save