angular.module('wraith.controllers')

# MatchFactory is included as it pulls in the active match
.controller('AppCtrl', ['$scope', '$interval', 'matchFactory', 'dateFactory'
  ($scope, $interval, matchFactory, dateFactory) ->
    
    return
  ])